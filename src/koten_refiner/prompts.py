DETECTOR_PROMPT = """次の文章は OCR の出力結果です。文字認識が誤っている部分にのみ<error>タグ</error>を付けてください。

注意:
・誤りのある語句だけを<error>タグ</error>で囲むこと。
・それ以外の補足説明、前置き、警告などは一切出力しないこと。
・タグ付きの文章だけを返してください。
"""

DETECTOR_SPAN_PROMPT = """次の文章は OCR の出力結果です。文字認識が誤っている範囲だけを JSON 配列で返してください。

出力形式:
[[開始位置, 終了位置]]

注意:
・start と end は Python の文字位置です。start は含み、end は含みません。
・誤りがない場合は [] を返してください。
・JSON 配列以外の説明、前置き、Markdown は一切出力しないでください。
"""

CORRECTOR_PROMPT = """次の文章は手書き文字の OCR 結果です。<error>タグで囲まれた箇所に OCR による誤りがあります。
<error>タグで囲まれた部分のみを訂正し，本来書かれていた通りの文字列に戻してください。
入力は，<error>タグ付きの文と，タグなしの文が<sep>で連結された形式です。
"""

ONE_STAGE_PROMPT = """次の文章は手書き文字の OCR 結果です。本来書かれていた通りの文字列に戻してください。"""

EDIT_ONLY_PROMPT = """次の文章は手書き文字の OCR 結果です。<error id="N">...</error> で囲まれた箇所だけを訂正してください。
出力は `id<TAB>訂正文` の形式で1行ずつ返してください。変更不要な箇所は `id<TAB><KEEP>` を返してください。
"""
